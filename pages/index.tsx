import React, { FC } from 'react';
import 'twin.macro';

const HomePage: FC = () => {
  return (
    <div tw="w-screen h-screen bg-red-500">
      <span>Home</span>
    </div>
  );
};

export default HomePage;
