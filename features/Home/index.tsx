import Layout from '@/components/Layout';
import React from 'react';
import 'twin.macro';

const Home = () => {
  return (
    <Layout>
      <div tw="font-bold">HOME</div>
    </Layout>
  );
};

export default Home;
