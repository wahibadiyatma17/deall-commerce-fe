import Layout from '@/components/Layout';
import React, { FC } from 'react';
import 'twin.macro';

const Cart: FC = () => {
  return (
    <Layout>
      <div tw="font-bold">Cart</div>
    </Layout>
  );
};

export default Cart;
